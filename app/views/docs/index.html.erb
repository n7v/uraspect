<% if @docs.any? %>
  <ul>
    <% @docs.each do |doc| %>
      <li>
        <%= link_to doc.name, doc_load_path(doc) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <div>
    Извините, на данный момент на сайте нет образцов документов, заявлений
  </div>
<% end %>

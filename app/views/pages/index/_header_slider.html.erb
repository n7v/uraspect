<div class="slider-inner">
  <div id="da-slider" class="da-slider">
    <% Banner.all.each do |banner| %>
      <%= render 'pages/index/slide', banner: banner %>
    <% end %>
    <nav class="da-arrows">
      <span class="da-arrows-prev"></span>
      <span class="da-arrows-next"></span>
    </nav>
  </div>
</div>

<%= semantic_form_for [:admin, resource] do |f| %>
  <%= f.inputs do %>
    <%= f.input :url %>
    <%= f.input :logo %>

    <%= image_tag resource.logo.thumb.url if resource.logo.thumb.url.present? %>
  <% end %>

  <%= f.actions %>
<% end %>
